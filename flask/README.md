### Generare ed eseguire un venv

```
python -m venv .venv

source .venv/bin/activate
```

### Eseguire l'app

```
flask --app nome_app run

Se il file si chiama `app.py` o `wsgi.py` il parametro `--app` non è necessario
```
